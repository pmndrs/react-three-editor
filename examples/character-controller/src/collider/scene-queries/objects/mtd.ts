export class MTD {
  public distance: number;
  public direction: THREE.Vector3;

  constructor(distance: number, direction: THREE.Vector3) {
    this.distance = distance;
    this.direction = direction;
  }
}
